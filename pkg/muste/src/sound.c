#include "muste.h"
#include <stdio.h>
#include "survolib.h"

int sound_on_off()
        {
		muste_fixme("FIXME: Sound functions not implemented! (sound_on_off)\n");

/*        int i;

        if (!sound_on && !soundbin_luettu)
            {
            i=sound_init();
            if (i<0) return(1);
            soundbin_luettu=1;
            }
        sound_on=1-sound_on;
*/        
        return(1);
        }

